# Auth (login)

Rutas: `/login`, `/recuperar`

Aquí va entrar, salir, recuperar contraseña y “¿esta persona puede ver esta pantalla?”.

- `pages/` — Login y recuperar
- `context/` — sesión (`AuthProvider`, `useAuth`)
- `guards/` — RequireAuth, RequireAdmin, RequireModule
